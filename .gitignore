# Runtime state — SECRETS. Never commit: contains the RSA private key + db.
data/

# Dependencies & build outputs (regenerated by the Docker build).
node_modules/
dist/
public/activate.js
public/pp-worker.js

# Local env (config only, but keep it out of git; see .env.example).
.env

*.log
npm-debug.log*
